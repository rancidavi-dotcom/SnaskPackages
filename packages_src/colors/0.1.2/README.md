# colors

Cores e estilos ANSI para terminal.

## Instalação

```bash
snask install colors
```

## Uso

```snask
import "colors"

class main
    fun start()
        print(colors::ok("OK"));
        print(colors::warn("WARN"));
        print(colors::err("ERROR"));
```
