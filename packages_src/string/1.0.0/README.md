# String (Snask Library)

Biblioteca primordial para manipulação de strings no Snask v0.3.0+. Oferece performance nativa através de implementações otimizadas no `runtime.c`.

## 🚀 Instalação

```bash
snask install string
```

## 🛠️ Funções (32)

*   `string::len(s)`: Retorna o comprimento da string.
*   `string::upper(s)`: Converte para maiúsculas.
*   `string::lower(s)`: Converte para minúsculas.
*   `string::trim(s)`: Remove espaços nas extremidades.
*   `string::split(s, sep)`: Divide a string em uma lista.
*   `string::join(items, sep)`: Junta uma lista em uma string.
*   `string::contains(s, sub)`: Verifica se contém uma substring.
*   `string::starts_with(s, pre)`: Verifica o prefixo.
*   `string::ends_with(s, suf)`: Verifica o sufixo.
*   `string::capitalize(s)`: Primeira letra em maiúscula.
*   `string::title(s)`: Todas as palavras com primeira letra maiúscula.
*   `string::reverse(s)`: Inverte a string.
*   `string::repeat(s, n)`: Repete a string N vezes.
*   `string::is_numeric(s)`: Verifica se contém apenas números.
*   ... e muito mais!

## 📄 Licença
Distribuído sob a Licença MIT.
